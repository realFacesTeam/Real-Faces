#!/usr/bin/env node
var debug = require('debug')('realTalk');
var app = require('../app');

var appPort = process.env.PORT || 3000;
app.set('port', appPort);
console.log(appPort);

var server = app.listen(app.get('port'), function() {
  debug('Express server listening on port ' + server.address().port);
});
